export class Programacion {
    start: Date;
    title: String;
    id: number;

    constructor(start:Date, title:string, id:number) {
        this.start = start;
        this.title = title;
        this.id = id;
    }
}